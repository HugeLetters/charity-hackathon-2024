<h2 class="root">
	<div class="hyphen"></div>
	<div><slot /></div>
</h2>

<style>
	.root {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		font-weight: 600;
		font-family: var(--font-jost);
		width: fit-content;
	}

	.hyphen {
		background-color: var(--color-brand-primary);
		width: 1rem;
		height: 0.1rem;
	}
</style>
